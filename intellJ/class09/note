inheritance: mechanism in java through which we can use the fields and methods from one class to other class without
writing a single line of code.
Benefits of using inheritance:
1) Cleaner more efficient code for memory and execution.
2) it helps to reduce code duplication
3) saves time and memory
4) reusing the same code again
Disadvantage of using inheritance:
1)if we change in parents class it will effect the child

SuperKeyword:
as we can use this keyword in java to refer to the current fields and methods we can use superkeyword
to access the fields and methods from immediate parent class.
it is used to subclasses to access superclass members
in inheritance when we create an object of a class all of the default constructor from